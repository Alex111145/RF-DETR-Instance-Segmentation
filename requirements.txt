--extra-index-url https://download.pytorch.org/whl/cu124

# GPU - PyTorch con CUDA 12.4 (compatibile con driver >= 525.x)
torch==2.6.0+cu124
torchvision==0.21.0+cu124

# Inferenza
rfdetr>=1.6.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Pre-processing / geo
rasterio>=1.3.0
